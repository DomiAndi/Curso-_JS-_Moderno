// Inicializar una variable con un valor
var producto = "Monitor de 20 pulgadas"
console.log(producto)

// Las variables se pueden reasifnar
producto = 'Monitor de 19 pulgadas';
console.log(producto);

// JS es un lenguaje dinamico
producto = 20;
console.log(producto);

// Se pueden inicializar y asignar valores despues 
var disponible;
disponible = true;

disponible = false;

// Inicializar multiples variables
var categoria = 'Computadoras';
    marca = "Marca famosa"
    calificacion = 5;