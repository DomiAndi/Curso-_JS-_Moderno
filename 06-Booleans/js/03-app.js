const autentificado = true;

// if(autentificado){
    console.log('Si puedes ver Amazon Prime')
//    }  else {
        console.log('No, no puedes verlo')
// }

// Operador ternario

console.log( autentificado ? 'Si esta autentificado' : 'No esta autentificado');